package com.sura.billing.selenium.pages.menu.acciones.escritorio;

import com.sura.guidewire.selenium.Guidewire;
import org.openqa.selenium.WebDriver;

public class DesembolsoPage extends Guidewire {
    public DesembolsoPage(WebDriver driver) {
        super(driver);
    }
}
